# 🐳 SnokHub - مدير Docker الاحترافي

<div align="center">

![SnokHub Logo](https://img.shields.io/badge/SnokHub-Docker%20Manager-blue?style=for-the-badge&logo=docker)

**أداة احترافية لإدارة Docker مع واجهة رسومية جميلة**

[التثبيت](#-التثبيت-السريع) • [المميزات](#-المميزات) • [الاستخدام](#-طرق-التشغيل) • [الدعم](#-الأنظمة-المدعومة)

</div>

---

## ✨ المميزات

### 🎯 **إدارة شاملة**
- ✅ إدارة الحاويات (تشغيل، إيقاف، حذف، السجلات)
- ✅ إدارة الصور المحلية
- ✅ تصفح وتحميل الصور من Docker Hub
- ✅ إدارة الشبكات والأحجام

### 💾 **النسخ الاحتياطي**
- ✅ نسخ احتياطي تلقائي لجميع الصور
- ✅ نسخ احتياطي لصورة محددة
- ✅ تصدير إعدادات الحاويات
- ✅ استعادة النسخ الاحتياطية بسهولة

### 🌐 **تصفح Docker Hub**
- ✅ البحث المباشر في Docker Hub
- ✅ عرض الصور الشائعة تلقائياً
- ✅ تحميل سريع بضغطة زر
- ✅ اختيار إصدارات مختلفة

### 🎨 **واجهة مستخدم**
- ✅ تصميم عربي جميل وحديث
- ✅ ألوان متدرجة جذابة
- ✅ تحديث تلقائي كل 5 ثواني
- ✅ إشعارات واضحة

---

## 🚀 التثبيت السريع

### الطريقة الأولى: تثبيت تلقائي (موصى به)

```bash
# تنزيل وتشغيل سكريبت التثبيت
curl -fsSL https://raw.githubusercontent.com/YOUR_REPO/install.sh | bash
```

### الطريقة الثانية: تثبيت يدوي

```bash
# 1. تنزيل السكريبت
wget https://raw.githubusercontent.com/YOUR_REPO/install.sh
# أو
curl -O https://raw.githubusercontent.com/YOUR_REPO/install.sh

# 2. جعله قابل للتنفيذ
chmod +x install.sh

# 3. تشغيل التثبيت
./install.sh
```

### الطريقة الثالثة: من الكود المصدري

```bash
# 1. حفظ سكريبت التثبيت
nano install.sh
# الصق محتوى السكريبت واحفظ (Ctrl+O ثم Ctrl+X)

# 2. جعله قابل للتنفيذ
chmod +x install.sh

# 3. تشغيل التثبيت
./install.sh
```

---

## 🖥️ الأنظمة المدعومة

السكريبت يكتشف النظام تلقائياً ويثبت المتطلبات المناسبة:

| التوزيعة | الحالة | مدير الحزم |
|---------|--------|-----------|
| Ubuntu | ✅ مدعوم | apt |
| Debian | ✅ مدعوم | apt |
| Linux Mint | ✅ مدعوم | apt |
| Pop!_OS | ✅ مدعوم | apt |
| Fedora | ✅ مدعوم | dnf |
| RHEL | ✅ مدعوم | dnf |
| CentOS | ✅ مدعوم | dnf |
| Arch Linux | ✅ مدعوم | pacman |
| Manjaro | ✅ مدعوم | pacman |
| OpenSUSE | ✅ مدعوم | zypper |

---

## 📋 المتطلبات

السكريبت يثبت جميع المتطلبات تلقائياً:

### متطلبات النظام
- Python 3.6+
- GTK 3
- Docker (يمكن تثبيته تلقائياً)

### مكتبات Python
- `docker` - للتفاعل مع Docker API
- `requests` - لتصفح Docker Hub
- `PyGObject` - للواجهة الرسومية
- `pycairo` - للرسومات

---

## 🎮 طرق التشغيل

بعد التثبيت، يمكنك تشغيل SnokHub بثلاث طرق:

### 1️⃣ من قائمة التطبيقات
- افتح قائمة التطبيقات
- ابحث عن **SnokHub**
- انقر للتشغيل

### 2️⃣ من سطح المكتب
- ابحث عن أيقونة **SnokHub** على سطح المكتب
- انقر مرتين للتشغيل

### 3️⃣ من Terminal
```bash
snokhub
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: "لا يمكن الاتصال بـ Docker"

**الحل:**
```bash
# 1. تأكد من تشغيل Docker
sudo systemctl start docker
sudo systemctl enable docker

# 2. إضافة المستخدم لمجموعة docker
sudo usermod -aG docker $USER

# 3. تسجيل الخروج وإعادة الدخول
# أو أعد تشغيل الجهاز
```

### المشكلة: "Permission denied"

**الحل:**
```bash
# تأكد من أن المستخدم في مجموعة docker
groups $USER

# إذا لم تظهر docker، أضف المستخدم
sudo usermod -aG docker $USER

# ثم سجل الخروج وأعد الدخول
```

### المشكلة: الأمر `snokhub` غير موجود

**الحل:**
```bash
# أعد تحميل ملف bashrc
source ~/.bashrc

# أو افتح Terminal جديد
```

### المشكلة: أخطاء في المكتبات

**الحل:**
```bash
# إعادة تثبيت مكتبات Python
pip3 install --user --upgrade docker requests PyGObject pycairo

# أو إعادة تشغيل السكريبت
./install.sh
```

---

## 🗑️ إلغاء التثبيت

لإلغاء تثبيت SnokHub:

```bash
./install.sh uninstall
```

أو يدوياً:

```bash
# حذف الملفات
rm -rf ~/.snokhub
rm -f ~/.local/share/applications/snokhub.desktop
rm -f ~/Desktop/snokhub.desktop
rm -f ~/.local/bin/snokhub

# النسخ الاحتياطية محفوظة في
# ~/docker_backups
```

---

## 📂 المجلدات والملفات

بعد التثبيت، سيتم إنشاء:

```
~/.snokhub/              # مجلد التثبيت الرئيسي
├── snokhub.py          # ملف البرنامج
└── snokhub.png         # الأيقونة

~/.local/share/applications/
└── snokhub.desktop     # اختصار التطبيق

~/Desktop/
└── snokhub.desktop     # اختصار سطح المكتب

~/.local/bin/
└── snokhub             # أمر Terminal

~/docker_backups/       # مجلد النسخ الاحتياطية
```

---

## 💡 نصائح الاستخدام

### 1. النسخ الاحتياطي المنتظم
```
- استخدم "💾 نسخ احتياطي سريع" بانتظام
- النسخ محفوظة في ~/docker_backups
- يمكن استعادتها بسهولة
```

### 2. تصفح Docker Hub
```
- انقر "⭐ الصور الشائعة" لرؤية أفضل الصور
- استخدم البحث للعثور على صور محددة
- اختر الإصدار المناسب قبل التحميل
```

### 3. إدارة الحاويات
```
- راجع السجلات لمعرفة حالة الحاوية
- استخدم إعادة التشغيل عند حدوث مشاكل
- احذف الحاويات غير المستخدمة
```

### 4. التنظيف الدوري
```
- استخدم زر "🧹 تنظيف" لحذف الموارد غير المستخدمة
- يحرر مساحة القرص
- يحسن الأداء
```

---

## 🆘 الحصول على المساعدة

إذا واجهت أي مشاكل:

1. **تحقق من السجلات:**
   ```bash
   journalctl -u docker
   docker info
   ```

2. **تأكد من الصلاحيات:**
   ```bash
   groups $USER
   docker ps
   ```

3. **أعد تشغيل Docker:**
   ```bash
   sudo systemctl restart docker
   ```

---

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

## 🌟 المساهمة

المساهمات مرحب بها! لا تتردد في:
- الإبلاغ عن المشاكل
- اقتراح ميزات جديدة
- تحسين الكود

---

## 📧 التواصل

للأسئلة والاقتراحات، يمكنك التواصل عبر:
- Issues على GitHub
- البريد الإلكتروني

---

<div align="center">

**صنع بـ ❤️ لمجتمع Docker العربي**

⭐ إذا أعجبك المشروع، لا تنسى النجمة على GitHub!

</div>
